services:
  api:
    build:
      context: ..
      dockerfile: dev/Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ..:/app
    environment:
      - NODE_ENV=development